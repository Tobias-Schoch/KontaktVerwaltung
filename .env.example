# KontaktHub Docker Configuration

# Port for the web interface (default: 8080)
PORT=8080

# Backend configuration (inside container)
# These are set in docker-compose.yml, but can be overridden here
# NODE_ENV=production
# DB_PATH=/app/data/kontakte.db
